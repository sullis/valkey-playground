<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.sullis</groupId>
  <artifactId>valkey-playground</artifactId>
  <packaging>jar</packaging>
  <name>valkey-playground</name>
  <version>0.0.1-SNAPSHOT</version>
  <description>valkey-playground</description>
  <licenses>
    <license>
      <name>Apache License 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0</url>
    </license>
  </licenses>

  <properties>
    <logback.version>1.6.3</logback.version>
    <slf4j.version>2.0.19</slf4j.version>
    <skip.tests>false</skip.tests>
    <!-- The bundle-wide instruction coverage the jacoco check execution enforces. The bundle is
         the fixtures under src/main/java and nothing else, so this is a floor on how much of them
         the suite exercises; the suite sits a little above it. It was 0.95 while the test classes
         were in the bundle too, inflating the ratio with code that is covered by definition.

         There is not much room left above this one. What the suite does not reach is the fixtures'
         own failure handling: the port search giving up, and the catch blocks in start() and
         close() that log and carry on. Reaching those means making a container or a client fail on
         demand, which is a mock rather than a test of anything Valkey does. So treat a drop below
         this as real, but do not read the remainder as work to be done. -->
    <jacoco.minimum.coverage>0.94</jacoco.minimum.coverage>
    <!-- An escape hatch for a partial run: fixture coverage is not a meaningful number when
         surefire was pointed at one class, so -Dtest=Foo wants this set too. -->
    <jacoco.check.skip>false</jacoco.check.skip>
  </properties>

    <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>2.0.5</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>6.1.3</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>


  <dependencies>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers</artifactId>
    </dependency>
    <dependency>
      <!-- A transitive of testcontainers, declared here because the fixtures import ExposedPort
           and Ports directly to bind a container port to the same port on the host. Its version
           is not managed by testcontainers-bom, so it is pinned here and tracks whatever
           testcontainers resolves. -->
      <groupId>com.github.docker-java</groupId>
      <artifactId>docker-java-api</artifactId>
      <version>3.7.1</version>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <version>4.0.0-M1</version>
    </dependency>
    <dependency>
      <groupId>org.awaitility</groupId>
      <artifactId>awaitility</artifactId>
      <version>4.3.0</version>
    </dependency>
    <dependency>
      <!-- The classifier-less artifact bundles the native library for every platform. The
           per-platform classifiers are smaller, but a classifier-qualified dependency is invisible
           to Dependabot, which is why this stayed on 1.2.0 for over a year. -->
      <groupId>io.valkey</groupId>
      <artifactId>valkey-glide</artifactId>
      <version>2.5.2</version>
    </dependency>
    <dependency>
      <!-- Annotations only: @Nullable has class retention and nothing reads it at runtime, so
           this is provided rather than compile. It also arrives transitively via valkey-glide,
           which is what it was compiling against before. -->
      <groupId>org.jspecify</groupId>
      <artifactId>jspecify</artifactId>
      <version>1.0.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
    </dependency>
    <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-engine</artifactId>
        <scope>test</scope>
    </dependency>
      <dependency>
          <groupId>org.junit.jupiter</groupId>
          <artifactId>junit-jupiter-params</artifactId>
          <scope>test</scope>
      </dependency>
    <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-core</artifactId>
        <version>${logback.version}</version>
        <scope>test</scope>
    </dependency>
    <dependency>
        <groupId>ch.qos.logback</groupId>
        <artifactId>logback-classic</artifactId>
        <version>${logback.version}</version>
        <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>3.6.3</version>
          <configuration>
          </configuration>
        </plugin>
      <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.16.0</version>
      </plugin>
      <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>3.12.0</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.6.0</version>
        <configuration>
          <forkCount>8</forkCount>
          <reuseForks>true</reuseForks>
        </configuration>
      </plugin>

      <!--
        Declared after surefire on purpose: the merge and report executions below are bound to
        the test phase, and goals within one phase run in the order their plugins appear here,
        so moving this above surefire would report on an execution file that does not exist yet.
      -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.15</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <!-- One execution file per fork. surefire runs up to forkCount JVMs at once and
                   expands ${surefire.forkNumber} per fork in the argLine this goal sets; a
                   single shared destFile would have eight JVMs appending to it concurrently. -->
              <destFile>${project.build.directory}/jacoco/fork-${surefire.forkNumber}.exec</destFile>
            </configuration>
          </execution>
          <execution>
            <id>merge</id>
            <phase>test</phase>
            <goals>
              <goal>merge</goal>
            </goals>
            <configuration>
              <fileSets>
                <fileSet>
                  <directory>${project.build.directory}/jacoco</directory>
                  <includes>
                    <include>fork-*.exec</include>
                  </includes>
                </fileSet>
              </fileSets>
              <destFile>${project.build.directory}/jacoco.exec</destFile>
            </configuration>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
            <configuration>
              <dataFile>${project.build.directory}/jacoco.exec</dataFile>
            </configuration>
          </execution>
          <execution>
            <id>check</id>
            <phase>test</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <skip>${jacoco.check.skip}</skip>
              <dataFile>${project.build.directory}/jacoco.exec</dataFile>
              <rules>
                <rule>
                  <!-- One rule over the whole bundle rather than a per-class one, so that a small
                       fixture with an uncovered branch or two does not have to clear the same bar
                       as the tree as a whole. -->
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <!-- Instructions, not branches. Branch coverage of this tree sits under
                           this floor already, largely on fixture error paths a passing run
                           never takes. -->
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.minimum.coverage}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>
</project>
